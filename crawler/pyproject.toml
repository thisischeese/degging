[project]
name = "naver-place-crawler"
version = "0.1.0"
description = "Naver Place crawler and cafe JSON enrichment pipeline"
requires-python = "==3.11.*"
dependencies = [
    "aiofiles>=25.1.0",
    "httpx>=0.28.1",
    "playwright>=1.58.0",
]

[tool.setuptools]
py-modules = []
